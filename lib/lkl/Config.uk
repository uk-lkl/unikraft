menuconfig LIBLKL
	bool "lkl: Linux Kernel Library"
	select HAVE_BOOTENTRY
	# depends on !LIBFDT && !LIBNOLIBC && !LIBUKALLOC && !LIBUKALLOCBBUDDY && !LIBUKARGPARSE && !LIBUKBOOT && !LIBUKBUS && !LIBUKDEBUG && !LIBUKLOCK && !LIBUKMPI && !LIBUKNETDEV && !LIBUKSCHED && !LIBUKSCHEDCOOP && !LIBUKSGLIST && !LIBUKSWRAND && !LIBUKTIMECONV && !LIBVFSCORE

if LIBLKL
endif
